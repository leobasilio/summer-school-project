uses data.ImageMetadata
uses ui.Image

data FaceAnonymizerResult {
  Image anonymizedImage
  int numberOfFaces
}

interface FaceAnonymizer {
  FaceAnonymizerResult anonymizeFaces(Image image, ImageMetadata imageMetadata)
}
