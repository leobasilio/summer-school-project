uses data.ImageMetadata
uses data.SpeedData
//uses media.WH
uses ui.Image

interface VehicleSpeedMeasurer {
  SpeedData measureSpeed(Image roadStructure, Image frames[], ImageMetadata framesMetadata[])
}
